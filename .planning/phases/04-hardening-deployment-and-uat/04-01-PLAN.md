---
phase: "04-hardening-deployment-and-uat"
plan: "01"
type: execute
wave: 1
depends_on:
  - "02-08"
  - "03-04"
files_modified:
  - "src/lib/qna/realtime.ts"
  - "src/lib/surveys/realtime.ts"
  - "src/components/qna/ConnectionStatus.tsx"
  - "src/components/qna/AudienceQuestionList.tsx"
  - "src/components/qna/ModeratorQueue.tsx"
  - "src/components/qna/PresenterView.tsx"
  - "src/components/surveys/SurveyPresentationView.tsx"
  - "tests/qna/realtime.test.ts"
  - "tests/surveys/realtime.test.ts"
  - "tests/e2e/qna-realtime.spec.ts"
  - "tests/e2e/survey-presentation.spec.ts"
autonomous: true
requirements:
  - LIVE-06
must_haves:
  truths:
    - "Live views show reconnecting and refresh-needed states."
    - "Browser offline state is visible and uses explicit text."
    - "Prolonged reconnect failure escalates to refresh-needed."
    - "Refresh-needed state includes a keyboard-operable Refresh view action."
    - "Realtime payloads remain refresh triggers only."
  artifacts:
    - path: "src/components/qna/ConnectionStatus.tsx"
      provides: "Shared live status banner with refresh action"
    - path: "src/lib/qna/realtime.ts"
      provides: "Q&A reconnect/offline escalation helper"
    - path: "src/lib/surveys/realtime.ts"
      provides: "Survey presentation reconnect/offline escalation helper"
---

<objective>
Harden live reconnect behavior for LIVE-06.

Purpose: Audience, moderator, presenter, and survey presentation users need clear status when live updates are delayed, offline, or unable to reconnect.
Output: Shared connection status banner, reconnect/offline escalation logic, wired refresh actions, and tests.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hardening-deployment-and-uat/04-CONTEXT.md
@.planning/phases/04-hardening-deployment-and-uat/04-UI-SPEC.md
@.planning/phases/04-hardening-deployment-and-uat/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-08-SUMMARY.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-04-SUMMARY.md
@src/components/qna/ConnectionStatus.tsx
@src/lib/qna/realtime.ts
@src/lib/surveys/realtime.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove reconnect/offline state contract</name>
  <files>tests/qna/realtime.test.ts, tests/surveys/realtime.test.ts, tests/e2e/qna-realtime.spec.ts, tests/e2e/survey-presentation.spec.ts</files>
  <behavior>
    - Test 1: subscription helpers expose `offline` in addition to `live`, `reconnecting`, and `refresh-needed`.
    - Test 2: `CLOSED` starts reconnecting and prolonged reconnect timeout escalates to refresh-needed.
    - Test 3: browser offline events surface offline status and online events allow recovery.
    - Test 4: refresh-needed UI includes the exact copy and a keyboard-focusable `Refresh view` button.
    - Test 5: audience/presenter/survey presentation surfaces do not render raw realtime payload content.
  </behavior>
  <action>Create failing unit and fixture E2E coverage before implementation. Use fake timers for timeout escalation and existing Playwright fixture events for browser-visible states.</action>
  <verify>
    <automated>npm test -- tests/qna/realtime.test.ts tests/surveys/realtime.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/qna-realtime.spec.ts tests/e2e/survey-presentation.spec.ts</automated>
  </verify>
  <done>Tests capture LIVE-06 and fail against the current thin badge-only behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared reconnect escalation</name>
  <files>src/lib/qna/realtime.ts, src/lib/surveys/realtime.ts</files>
  <behavior>
    - Test 1: helpers call `onConnectionChange("reconnecting")` on closed/interrupted channels.
    - Test 2: helpers call `onConnectionChange("refresh-needed")` after the prolonged reconnect threshold.
    - Test 3: helpers call `onConnectionChange("offline")` when `navigator.onLine` is false or the browser fires `offline`.
    - Test 4: cleanup removes channels, timers, intervals, and browser event listeners.
  </behavior>
  <action>Add a small shared internal connection-state utility if it avoids duplicating timer/offline logic. Keep Supabase events as refresh triggers only. Default the prolonged reconnect threshold to 30 seconds while allowing tests to override it.</action>
  <verify>
    <automated>npm test -- tests/qna/realtime.test.ts tests/surveys/realtime.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Realtime helpers support LIVE-06 without adding offline mutation queues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Upgrade live status banner and wire refresh actions</name>
  <files>src/components/qna/ConnectionStatus.tsx, src/components/qna/AudienceQuestionList.tsx, src/components/qna/ModeratorQueue.tsx, src/components/qna/PresenterView.tsx, src/components/surveys/SurveyPresentationView.tsx</files>
  <behavior>
    - Test 1: `ConnectionStatus` renders state-specific copy, tone, aria-live, and refresh action only for refresh-needed.
    - Test 2: refresh action calls the relevant `router.refresh()` or page refresh callback.
    - Test 3: moderator mutating controls are disabled when browser offline, while realtime-only degradation remains a warning.
    - Test 4: 360px mobile surfaces have no horizontal overflow with reconnect banners visible.
  </behavior>
  <action>Convert the status badge into a compact banner that still fits existing headers. Pass refresh callbacks from live surfaces. Preserve existing public/presenter safety boundaries and avoid rendering raw realtime payloads.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/qna-realtime.spec.ts tests/e2e/audience-qna.spec.ts tests/e2e/moderation.spec.ts tests/e2e/presenter-view.spec.ts tests/e2e/survey-presentation.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>All live surfaces show actionable reconnect hardening states and remain mobile-accessible.</done>
</task>

</tasks>

<verification>
Run:
- npm test -- tests/qna/realtime.test.ts tests/surveys/realtime.test.ts
- npm run test:e2e -- tests/e2e/qna-realtime.spec.ts tests/e2e/audience-qna.spec.ts tests/e2e/moderation.spec.ts tests/e2e/presenter-view.spec.ts tests/e2e/survey-presentation.spec.ts
- npm run lint
- npx tsc --noEmit
</verification>

<success_criteria>
LIVE-06 is implemented across live views: reconnecting/offline/refresh-needed states are visible, prolonged failure prompts refresh, and safe server refresh boundaries remain intact.
</success_criteria>

<output>
Create `.planning/phases/04-hardening-deployment-and-uat/04-01-SUMMARY.md` when done.
</output>
