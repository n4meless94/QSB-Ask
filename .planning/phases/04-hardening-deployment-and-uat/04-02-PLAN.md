---
phase: "04-hardening-deployment-and-uat"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "next.config.ts"
  - "Dockerfile"
  - ".dockerignore"
  - "src/app/api/health/route.ts"
  - "tests/e2e/foundation.spec.ts"
  - "README.md"
  - ".planning/deployment/coolify-runbook.md"
autonomous: true
requirements:
  - DEPL-02
  - DEPL-03
  - DEPL-04
must_haves:
  truths:
    - "The health route is suitable for Coolify verification and never prints secret values."
    - "The app has a Coolify-compatible Next.js production deployment path."
    - "The deployment runbook requires a Coolify-managed application resource on QSB VPS."
    - "The runbook prepares the `https://ask.qsbportal.com.my` DNS/Coolify cutover."
  artifacts:
    - path: "src/app/api/health/route.ts"
      provides: "Non-secret liveness/readiness response for Coolify"
    - path: "Dockerfile"
      provides: "Standalone Next.js container build path for Coolify Dockerfile mode"
    - path: ".planning/deployment/coolify-runbook.md"
      provides: "QSB Ask deployment and domain cutover checklist"
---

<objective>
Make QSB Ask deployment-ready for Coolify and the production URL.

Purpose: QSB operators need a documented and verifiable way to deploy the Next.js app as a Coolify-managed resource on QSB VPS.
Output: Health readiness behavior, standalone deployment config, Docker/Coolify runbook, README updates, and verification tests.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hardening-deployment-and-uat/04-CONTEXT.md
@.planning/phases/04-hardening-deployment-and-uat/RESEARCH.md
@src/app/api/health/route.ts
@src/lib/env.ts
@next.config.ts
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove health readiness and secret-safe deployment contract</name>
  <files>tests/e2e/foundation.spec.ts, src/app/api/health/route.ts</files>
  <behavior>
    - Test 1: `/api/health` returns service, environment, timestamp, and configuration status without secret values.
    - Test 2: development/test health remains reachable when env placeholders are incomplete.
    - Test 3: production readiness returns a non-OK status when required runtime keys are missing.
    - Test 4: missing configuration output includes key names only.
  </behavior>
  <action>Extend foundation health tests first. If direct production-mode route testing is awkward in Playwright, add a small exported health response builder that can be unit-tested without leaking secrets.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/foundation.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>DEPL-02 is expressed as tests before route changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add standalone Next.js/Coolify deployment configuration</name>
  <files>next.config.ts, Dockerfile, .dockerignore, package.json</files>
  <behavior>
    - Test 1: Next.js config enables standalone output for Docker-style self-hosting.
    - Test 2: Dockerfile exposes port 3000, runs as a non-root user, sets `HOSTNAME=0.0.0.0`, and starts `server.js`.
    - Test 3: Docker build context excludes `.env*`, `.next`, `node_modules`, test artifacts, and planning-only clutter where safe.
    - Test 4: `npm run build` succeeds.
  </behavior>
  <action>Follow official Next.js standalone output guidance. Keep Coolify as the deployment owner: the Dockerfile is a build artifact for Coolify Dockerfile mode, not an unmanaged VPS service.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The app can build as a standalone self-hosted Next.js application suitable for Coolify.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write Coolify deployment and domain cutover runbook</name>
  <files>README.md, .planning/deployment/coolify-runbook.md</files>
  <behavior>
    - Checklist requires a Coolify application resource on QSB VPS.
    - Checklist lists required runtime env keys without values.
    - Checklist includes health path `/api/health`, exposed port 3000, HTTPS route, DNS target, rollback, and post-deploy smoke checks.
    - Checklist names `https://ask.qsbportal.com.my` as the production public URL target.
  </behavior>
  <action>Create a concise operator runbook under `.planning/deployment/` and update README with the canonical pointer. Include official-source notes for Next.js standalone and Coolify health/env behavior.</action>
  <verify>
    <automated>rg -n "ask.qsbportal.com.my|/api/health|Coolify|NEXT_PUBLIC_SUPABASE_URL" README.md .planning/deployment/coolify-runbook.md</automated>
  </verify>
  <done>DEPL-03 and DEPL-04 are documented with concrete operator checks.</done>
</task>

</tasks>

<verification>
Run:
- npm run build
- npm run test:e2e -- tests/e2e/foundation.spec.ts
- npm run lint
- npx tsc --noEmit
</verification>

<success_criteria>
DEPL-02 through DEPL-04 have code/config/docs evidence: Coolify can check health, build the app, and follow a production URL cutover runbook.
</success_criteria>

<output>
Create `.planning/phases/04-hardening-deployment-and-uat/04-02-SUMMARY.md` when done.
</output>
