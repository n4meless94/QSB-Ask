---
phase: 02-live-event-qna-and-moderation
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-04"]
files_modified:
  - "supabase/migrations/202605220202_qna_moderation.sql"
  - "src/lib/qna/moderation.ts"
  - "src/app/(app)/events/[eventId]/qna-actions.ts"
  - "src/components/qna/ModeratorQueue.tsx"
  - "src/components/qna/ModerationHistoryPanel.tsx"
  - "tests/qna/moderation.test.ts"
  - "tests/e2e/moderation.spec.ts"
autonomous: true
requirements: [AUTH-07, QNA-04, QNA-05, QNA-06, QNA-07, QNA-08, QNA-09, QNA-15]
user_setup: []
must_haves:
  truths:
    - "Moderator can review pending/live/answered/archived queues for assigned events only"
    - "Moderator actions use first-action-wins semantics and record audit rows"
    - "Edits preserve question versions and original text"
  artifacts:
    - path: "src/lib/qna/moderation.ts"
      provides: "Moderation list and action helpers"
      exports: ["listModerationQuestions", "approveQuestion", "dismissQuestion", "editQuestion", "archiveQuestion", "restoreQuestion", "markQuestionAnswered"]
    - path: "src/components/qna/ModeratorQueue.tsx"
      provides: "Staff moderation queue UI"
    - path: "src/components/qna/ModerationHistoryPanel.tsx"
      provides: "Staff-only action/version history"
  key_links:
    - from: "src/lib/qna/moderation.ts"
      to: "src/lib/events/access.ts"
      via: "getModeratorEventAccess/assertEventRole"
      pattern: "(getModeratorEventAccess|assertEventRole)"
    - from: "src/lib/qna/moderation.ts"
      to: "moderation_actions"
      via: "audit insert"
      pattern: "moderation_actions"
---

<objective>
Deliver the staff moderation queue and audited moderation actions.

Purpose: The moderator workflow is the control point that prevents inappropriate or sensitive questions from becoming public.
Output: Moderation helpers/actions/UI, history panel, migration support, and tests.
</objective>

## Phase Goal

**As a** QSB event organiser or moderator, **I want to** collect questions through a join link, review them before they appear publicly, and show approved questions to participants and speakers, **so that** live event Q&A stays controlled and useful.

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-event-qna-and-moderation/02-CONTEXT.md
@.planning/phases/02-live-event-qna-and-moderation/02-UI-SPEC.md
@.planning/phases/02-live-event-qna-and-moderation/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-SOURCE-AUDIT.md
@.planning/phases/02-live-event-qna-and-moderation/02-01-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-04-SUMMARY.md
@src/lib/events/access.ts
@src/lib/qna/public.ts
@supabase/migrations/202605220101_foundation_schema.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove moderation actions and queue behavior</name>
  <files>tests/qna/moderation.test.ts, tests/e2e/moderation.spec.ts</files>
  <behavior>
    - Approve moves pending to live.
    - Dismiss moves any allowed question to archived and records action dismiss.
    - Edit writes next question_versions row, sets is_edited, and records action edit.
    - Archive sets previous_status and archived; restore returns to previous_status.
    - Mark answered moves live to answered.
    - Stale expected status/updated_at returns exact stale-state error.
    - Search checks question text only; sort supports most recent, oldest, most votes.
  </behavior>
  <action>Create failing tests for QNA-04 through QNA-09 and QNA-15. Include a regression assertion that presenter/public helpers still cannot select pending/archived data after moderation actions. E2E should cover queue tabs, search, sort, action success, edit marker, history panel, and stale-state copy.</action>
  <verify>
    <automated>npm run test -- tests/qna/moderation.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/moderation.spec.ts</automated>
  </verify>
  <done>Tests define all moderation state transitions, audit requirements, and stale-action behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement compare-and-set moderation helpers and actions</name>
  <files>supabase/migrations/202605220202_qna_moderation.sql, src/lib/qna/moderation.ts, src/app/(app)/events/[eventId]/qna-actions.ts</files>
  <behavior>
    - Helpers require organiser/moderator access through server-side checks.
    - State changes compare expected status and expected updated_at.
    - Every action inserts `moderation_actions` with actor_user_id, action, from_status, to_status, metadata, created_at.
    - Edits preserve original/current text by adding `question_versions`.
  </behavior>
  <action>Implement transaction-safe moderation paths using SQL functions/RPC or a single database-safe sequence that cannot silently overwrite a newer state. Return the stale-state UI-SPEC error when no row matches expected status/updated_at. Keep presenter/public reads separate and approved-only per D-02 and D-06.</action>
  <verify>
    <automated>npm run test -- tests/qna/moderation.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>All moderation actions are server-authorized, audited, race-aware, and version-preserving.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build moderator queue and history UI</name>
  <files>src/components/qna/ModeratorQueue.tsx, src/components/qna/ModerationHistoryPanel.tsx</files>
  <behavior>
    - Queue has Pending, Live, Answered, Archived tabs with counts.
    - Rows show text, identity/Anonymous, submitted time, vote count, status, edited marker, and allowed actions.
    - Edit opens text area and saves/cancels without losing focus.
    - History panel is staff-only and never appears in participant/presenter views.
  </behavior>
  <action>Build dense work UI per Phase 2 UI-SPEC. Actions must disable while pending and display stale-state/action failure messages. Do not render presenter controls, participant submit controls, survey controls, export controls, or Phase 4 offline action queuing.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/moderation.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Assigned moderators can manage questions from the workspace and all actions update the queue with clear success/error states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser to moderation action | Moderator submits action on event data |
| moderation action to database | Status changes and audit rows must be atomic |
| staff queue to public helpers | Private moderation data must not cross into public surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | moderation state changes | mitigate | Compare expected status/updated_at and return stale-state error on mismatch. |
| T-02-05-02 | Repudiation | moderation audit | mitigate | Insert `moderation_actions` for every action with actor/status/metadata. |
| T-02-05-03 | Elevation of Privilege | qna actions | mitigate | Require organiser/moderator access before list or mutate. |
| T-02-05-04 | Information Disclosure | history panel | mitigate | Render only in staff queue and never import into public/presenter components. |
</threat_model>

<verification>
Run moderation unit tests, moderation E2E tests, lint, and `npx supabase db reset` if migration changes apply.
</verification>

<success_criteria>
AUTH-07 and QNA-04 through QNA-09 plus QNA-15 are complete with audited, race-aware moderation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-event-qna-and-moderation/02-05-SUMMARY.md`.
</output>
