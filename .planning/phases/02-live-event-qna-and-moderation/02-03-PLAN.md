---
phase: 02-live-event-qna-and-moderation
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "src/lib/participants/session.ts"
  - "src/lib/participants/validation.ts"
  - "src/app/join/[[...joinCode]]/page.tsx"
  - "src/app/join/actions.ts"
  - "src/components/participants/ParticipantJoinForm.tsx"
  - "tests/qna/participant-session.test.ts"
  - "tests/e2e/participant-join.spec.ts"
autonomous: true
requirements: [EVNT-06, EVNT-07]
user_setup: []
must_haves:
  truths:
    - "Participant can join an active event by code or shared link"
    - "Identity fields match the event identity mode"
    - "Participant session uses an HTTP-only cookie with only a hashed token stored in the database"
  artifacts:
    - path: "src/lib/participants/session.ts"
      provides: "Token generation, hashing, join, and session validation helpers"
      exports: ["joinParticipantEvent", "validateParticipantSession", "getParticipantCookieName"]
    - path: "src/app/join/[[...joinCode]]/page.tsx"
      provides: "Public join route by code or shared link"
    - path: "src/components/participants/ParticipantJoinForm.tsx"
      provides: "Mobile-first identity-aware join form"
  key_links:
    - from: "src/app/join/actions.ts"
      to: "src/lib/participants/session.ts"
      via: "joinParticipantEvent"
      pattern: "joinParticipantEvent"
    - from: "src/lib/participants/session.ts"
      to: "participant_sessions.session_token_hash"
      via: "hash before insert"
      pattern: "session_token_hash"
---

<objective>
Deliver participant join and secure event-scoped session identity.

Purpose: All participant submission and voting depends on proving possession of a session token, not trusting a client-supplied session id.
Output: Join route/action, token helpers, mobile join form, and tests.
</objective>

## Phase Goal

**As a** QSB event organiser or moderator, **I want to** collect questions through a join link, review them before they appear publicly, and show approved questions to participants and speakers, **so that** live event Q&A stays controlled and useful.

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-event-qna-and-moderation/02-CONTEXT.md
@.planning/phases/02-live-event-qna-and-moderation/02-UI-SPEC.md
@.planning/phases/02-live-event-qna-and-moderation/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-SOURCE-AUDIT.md
@src/lib/events/events.ts
@src/lib/supabase/server.ts
@supabase/migrations/202605220101_foundation_schema.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove join and participant token behavior</name>
  <files>tests/qna/participant-session.test.ts, tests/e2e/participant-join.spec.ts</files>
  <behavior>
    - Valid active join code creates a participant session.
    - Draft, ended, archived, and unknown events cannot be joined.
    - Anonymous mode requires no identity fields.
    - Name-required mode requires display name.
    - Name-email-required mode requires display name and valid email.
    - Raw token is never stored; cookie is HTTP-only, SameSite=Lax, event-scoped.
  </behavior>
  <action>Create failing tests for EVNT-06 and EVNT-07. Unit tests should assert token hashing and validation without exposing raw token. E2E should cover `/join`, `/join/QSB2X9ZA`, invalid code copy, identity-required copy, and 360px mobile no-overflow per D-04.</action>
  <verify>
    <automated>npm run test -- tests/qna/participant-session.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/participant-join.spec.ts</automated>
  </verify>
  <done>Tests fail until secure join/session behavior exists and cover all identity modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement participant session helpers and join action</name>
  <files>src/lib/participants/session.ts, src/lib/participants/validation.ts, src/app/join/actions.ts</files>
  <behavior>
    - `joinParticipantEvent` resolves code case-insensitively to an active event.
    - It validates identity mode and lowercases email.
    - It generates a high-entropy raw token, stores only a hash, and returns cookie metadata.
    - `validateParticipantSession` verifies raw token against hash and event id before later writes.
  </behavior>
  <action>Use Node crypto or Web Crypto primitives; do not hand-roll cryptography. Insert into `participant_sessions` through the server path and set the participant cookie only inside the server action. Do not use localStorage or expose `participant_sessions.id` as proof of identity. Follow D-04 exactly.</action>
  <verify>
    <automated>npm run test -- tests/qna/participant-session.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Join helper creates and validates event-scoped participant sessions using raw-token possession and hashed storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build public join screen</name>
  <files>src/app/join/[[...joinCode]]/page.tsx, src/components/participants/ParticipantJoinForm.tsx</files>
  <behavior>
    - Join form resolves shared link code and shows event name.
    - Required identity fields appear based on event identity mode.
    - Successful join redirects to the event Q&A route.
    - Invalid, not active, and closed events show participant-safe errors.
  </behavior>
  <action>Build a public route outside `(app)` without admin shell. Keep copy and states aligned to Phase 2 UI-SPEC. The page must not render organiser controls, member data, private event settings, or Phase 3 survey tabs.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/participant-join.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Participants can join active events from code/link on mobile, receive a secure cookie, and are sent to Q&A.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser to join action | Anonymous participant submits join code and identity |
| join action to database | Server creates session and stores hashed token |
| browser cookie to later actions | Cookie proves possession of raw token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Spoofing | participant session | mitigate | Generate high-entropy raw token, store only hash, validate raw token before writes. |
| T-02-03-02 | Information Disclosure | join route | mitigate | Public route returns only participant-safe event details. |
| T-02-03-03 | Tampering | identity fields | mitigate | Server-side identity-mode validation and email normalization. |
| T-02-03-04 | Denial of Service | join attempts | accept | No external rate limiter in Phase 2; question submission rate limit is enforced in Plan 02-04. |
</threat_model>

<verification>
Run focused participant session tests, participant join E2E tests, and lint.
</verification>

<success_criteria>
EVNT-06 and EVNT-07 are complete with secure event-scoped participant sessions and no admin data exposed on public routes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-event-qna-and-moderation/02-03-SUMMARY.md`.
</output>
