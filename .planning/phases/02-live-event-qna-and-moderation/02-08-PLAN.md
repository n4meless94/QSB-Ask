---
phase: 02-live-event-qna-and-moderation
plan: 08
type: execute
wave: 6
depends_on: ["02-05", "02-06", "02-07"]
files_modified:
  - "supabase/migrations/202605220204_qna_realtime.sql"
  - "src/lib/qna/realtime.ts"
  - "src/components/qna/ConnectionStatus.tsx"
  - "src/components/qna/ModeratorQueue.tsx"
  - "src/components/qna/AudienceQuestionList.tsx"
  - "src/components/qna/PresenterView.tsx"
  - "tests/e2e/qna-realtime.spec.ts"
  - "tests/e2e/qna-flow.spec.ts"
autonomous: true
requirements: [QNA-03, LIVE-01, LIVE-02, LIVE-04]
user_setup: []
must_haves:
  truths:
    - "Moderator queue updates pending count and status changes within 2 seconds in normal conditions"
    - "Audience Q&A updates approved questions, votes, and answered state within 2 seconds"
    - "Presenter View updates approved questions, votes, and status within 2 seconds"
    - "Public and presenter realtime paths never receive or render pending/archived payloads"
  artifacts:
    - path: "supabase/migrations/202605220204_qna_realtime.sql"
      provides: "Realtime publication entries for Q&A tables"
    - path: "src/lib/qna/realtime.ts"
      provides: "Audience-specific realtime subscription helpers"
      exports: ["subscribeToModeratorQuestions", "subscribeToPublicQuestions"]
    - path: "src/components/qna/ConnectionStatus.tsx"
      provides: "Accessible live/reconnecting/refresh-needed indicator"
  key_links:
    - from: "src/lib/qna/realtime.ts"
      to: "src/lib/qna/public.ts"
      via: "public/presenter callbacks refetch safe list"
      pattern: "listPublicQuestions"
    - from: "src/components/qna/ModeratorQueue.tsx"
      to: "subscribeToModeratorQuestions"
      via: "staff channel after role verification"
      pattern: "subscribeToModeratorQuestions"
    - from: "src/components/qna/AudienceQuestionList.tsx"
      to: "subscribeToPublicQuestions"
      via: "safe public channel"
      pattern: "subscribeToPublicQuestions"
---

<objective>
Deliver normal-condition realtime updates and cross-surface integration verification.

Purpose: Live Q&A is usable only when moderators, participants, and speakers see approved state changes quickly without private payload leaks.
Output: Realtime migration/helpers, connection status UI, wired components, and integration tests.
</objective>

## Phase Goal

**As a** QSB event organiser or moderator, **I want to** collect questions through a join link, review them before they appear publicly, and show approved questions to participants and speakers, **so that** live event Q&A stays controlled and useful.

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-event-qna-and-moderation/02-CONTEXT.md
@.planning/phases/02-live-event-qna-and-moderation/02-UI-SPEC.md
@.planning/phases/02-live-event-qna-and-moderation/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-SOURCE-AUDIT.md
@.planning/phases/02-live-event-qna-and-moderation/02-05-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-06-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-07-SUMMARY.md
@src/lib/qna/public.ts
@src/lib/qna/moderation.ts
@src/components/qna/ModeratorQueue.tsx
@src/components/qna/AudienceQuestionList.tsx
@src/components/qna/PresenterView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove realtime and no-leak integration behavior</name>
  <files>tests/e2e/qna-realtime.spec.ts, tests/e2e/qna-flow.spec.ts</files>
  <behavior>
    - Submit pending question: moderator pending count updates within 2 seconds; audience and presenter do not show pending text.
    - Approve question: audience and presenter show it within 2 seconds.
    - Upvote question: audience and presenter vote counts update within 2 seconds.
    - Mark answered: audience and presenter status updates within 2 seconds.
    - Archive/dismiss question: audience and presenter remove it or keep only allowed answered/live rows; no private statuses render.
  </behavior>
  <action>Create failing integration tests for LIVE-01, LIVE-02, LIVE-04, and the QNA-03 leak invariant. Use normal-condition subscriptions and basic connection states only. Do not add prolonged reconnect hardening, offline action queues, deployment checks, survey realtime, or export checks.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/qna-realtime.spec.ts tests/e2e/qna-flow.spec.ts</automated>
  </verify>
  <done>Integration tests fail until realtime wiring exists and assert the 2-second target plus no private payload rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Q&A realtime publication and subscription helpers</name>
  <files>supabase/migrations/202605220204_qna_realtime.sql, src/lib/qna/realtime.ts, src/components/qna/ConnectionStatus.tsx</files>
  <behavior>
    - Migration adds required Q&A tables to Supabase Realtime publication.
    - Staff subscriptions are separate from public/presenter subscriptions.
    - Public/presenter callbacks refetch sanitized live/answered list instead of trusting raw row payloads.
    - Connection indicator supports Live, Reconnecting, and Refresh needed copy.
  </behavior>
  <action>Add realtime publication entries for `questions` and `question_votes`; include `moderation_actions` only for staff-safe moderation refresh if needed. Build helpers that subscribe by event id and audience type. Public and presenter paths must never subscribe to or store pending/archived row payloads; refetch via `listPublicQuestions` after relevant change notifications per D-09.</action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npx supabase db reset</automated>
  </verify>
  <done>Realtime infrastructure exists and subscription helpers enforce staff/public/presenter separation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire realtime into moderation, audience, and presenter surfaces</name>
  <files>src/components/qna/ModeratorQueue.tsx, src/components/qna/AudienceQuestionList.tsx, src/components/qna/PresenterView.tsx</files>
  <behavior>
    - Moderator queue updates counts/statuses through staff-safe refresh.
    - Audience list updates approved questions and vote counts through public-safe refresh.
    - Presenter View updates approved questions and vote counts through public-safe refresh.
    - Reconnecting disables moderation actions with "Reconnect to continue" but does not implement offline queues.
  </behavior>
  <action>Connect the realtime helpers to existing components. Use ARIA-live polite announcements for connection/list state without announcing every vote count change. Keep public and presenter client state shaped by safe helpers only; do not client-filter private payloads after receiving them.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/qna-realtime.spec.ts tests/e2e/qna-flow.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Q&A state, votes, moderation status, and presenter updates appear within 2 seconds under normal conditions with no private status leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database changes to realtime | Row changes can contain private data if subscribed unsafely |
| realtime callback to browser state | Client state must remain audience-safe |
| browser connection state to actions | Offline moderation actions must be rejected in Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Information Disclosure | public/presenter realtime | mitigate | Separate subscription helpers and sanitized refetch via public helper. |
| T-02-08-02 | Information Disclosure | staff realtime channel | mitigate | Staff channel only after server role verification and only in moderation UI. |
| T-02-08-03 | Tampering | offline moderation | mitigate | Disable moderation actions while reconnecting; no offline queue in Phase 2. |
| T-02-08-04 | Denial of Service | realtime event bursts | accept | Normal-condition 300-participant target; prolonged reconnect/load hardening remains Phase 4. |
</threat_model>

<verification>
Run `npm run test:e2e -- tests/e2e/qna-realtime.spec.ts tests/e2e/qna-flow.spec.ts`, `npm run lint`, and `npx supabase db reset`.
</verification>

<success_criteria>
LIVE-01, LIVE-02, and LIVE-04 are complete, and QNA-03 remains enforced during realtime updates.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-event-qna-and-moderation/02-08-SUMMARY.md`.
</output>
