---
phase: 02-live-event-qna-and-moderation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/events/access.ts"
  - "src/app/(app)/events/[eventId]/page.tsx"
  - "src/app/(app)/events/[eventId]/access-actions.ts"
  - "src/components/events/EventWorkspace.tsx"
  - "src/components/events/EventAccessPanel.tsx"
  - "tests/events/event-access.test.ts"
  - "tests/e2e/event-workspace.spec.ts"
autonomous: true
requirements: [AUTH-05, AUTH-06, AUTH-07]
user_setup: []
must_haves:
  truths:
    - "Organiser can open an Event Workspace for an accessible event"
    - "Organiser can add pending moderator or speaker access and remove non-owner access"
    - "Moderator and speaker route access is determined by assigned active event role"
  artifacts:
    - path: "src/lib/events/access.ts"
      provides: "Role assertion and member management helpers"
      exports: ["assertEventRole", "listEventMembersForOrganiser", "inviteEventMember", "removeEventMember", "getPresenterEventAccess", "getModeratorEventAccess"]
    - path: "src/components/events/EventWorkspace.tsx"
      provides: "Workspace shell with Q&A, Access, Settings, Presenter tabs"
    - path: "src/components/events/EventAccessPanel.tsx"
      provides: "Organiser member list, invite, and remove UI"
  key_links:
    - from: "src/app/(app)/events/[eventId]/page.tsx"
      to: "src/lib/events/access.ts"
      via: "assertEventRole"
      pattern: "assertEventRole"
    - from: "src/app/(app)/events/[eventId]/access-actions.ts"
      to: "src/lib/events/access.ts"
      via: "inviteEventMember/removeEventMember"
      pattern: "(inviteEventMember|removeEventMember)"
---

<objective>
Create the authenticated Event Workspace access slice.

Purpose: Phase 2 needs staff-role separation before moderation and presenter surfaces exist, because role escalation would compromise the core visibility rule.
Output: Role helpers, workspace shell, access-management UI/actions, and focused unit/E2E coverage.
</objective>

## Phase Goal

**As a** QSB event organiser or moderator, **I want to** collect questions through a join link, review them before they appear publicly, and show approved questions to participants and speakers, **so that** live event Q&A stays controlled and useful.

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-event-qna-and-moderation/02-CONTEXT.md
@.planning/phases/02-live-event-qna-and-moderation/02-UI-SPEC.md
@.planning/phases/02-live-event-qna-and-moderation/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-SOURCE-AUDIT.md
@.planning/phases/01-foundation-auth-and-data/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth-and-data/01-04-SUMMARY.md
@src/lib/events/events.ts
@src/lib/supabase/rls.ts
@src/app/(app)/events/[eventId]/page.tsx
@supabase/migrations/202605220101_foundation_schema.sql

<interfaces>
Existing contracts: `EventRole` is `organiser | moderator | speaker`; `MemberStatus` is `invited | active | removed`; `EVENT_MANAGEMENT_ROLES`, `MODERATION_ROLES`, and `PRESENTER_ROLES` already live in `src/lib/supabase/rls.ts`. Extend Phase 1 event helper patterns; do not create a parallel auth/session layer per D-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove role helper and workspace access behavior</name>
  <files>tests/events/event-access.test.ts, tests/e2e/event-workspace.spec.ts</files>
  <behavior>
    - Test 1: organiser access allows workspace and access panel rendering.
    - Test 2: moderator access allows moderation access helper but not organiser member management.
    - Test 3: speaker access allows presenter access helper but not moderation tools.
    - Test 4: invite creates an invited moderator/speaker row and copy says manual onboarding, not email sent.
    - Test 5: original organiser removal is blocked.
  </behavior>
  <action>Create failing Vitest and Playwright tests for AUTH-05, AUTH-06, and AUTH-07. Mock Supabase chains the same way `tests/events/events.test.ts` does. E2E should use `QSB_ASK_E2E_AUTH=1` and assert `/events/event-1` shows workspace tabs plus Access content. Reference D-01 and D-03 in test names or descriptions.</action>
  <verify>
    <automated>npm run test -- tests/events/event-access.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/event-workspace.spec.ts</automated>
  </verify>
  <done>Tests fail for missing helpers/routes before implementation and express role separation without relying on client-only checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement event role helpers and access actions</name>
  <files>src/lib/events/access.ts, src/app/(app)/events/[eventId]/access-actions.ts</files>
  <behavior>
    - `assertEventRole(userId,eventId,allowedRoles)` returns role context or throws access-denied.
    - `listEventMembersForOrganiser(eventId)` requires organiser role.
    - `inviteEventMember(eventId,email,role)` permits only moderator/speaker pending records and lowercases email.
    - `removeEventMember(eventId,memberId)` marks access removed and refuses original organiser removal.
    - `getPresenterEventAccess` and `getModeratorEventAccess` enforce D-03 role sets.
  </behavior>
  <action>Build server-only helpers using `createSupabaseServerClient`, `event_members`, and `users` joins where needed. Server actions must get the signed-in Supabase user, call the helpers, return form/action state, and never import the admin client into browser code. Implement pending member records per D-03; do not claim email delivery. Preserve Phase 1 profile/membership patterns from `src/lib/events/events.ts`.</action>
  <verify>
    <automated>npm run test -- tests/events/event-access.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Role helpers pass tests; organiser can invite/remove member records; moderator/speaker access checks are explicit and server-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace event detail deferral with workspace access UI</name>
  <files>src/app/(app)/events/[eventId]/page.tsx, src/components/events/EventWorkspace.tsx, src/components/events/EventAccessPanel.tsx</files>
  <behavior>
    - Workspace renders one h1, event status, join code/link copy, Presenter action, and Q&A/Access/Settings/Presenter tabs.
    - Access tab lists members with role/status badges and remove controls for organiser only.
    - Moderator and speaker users never see organiser-only access controls.
    - UI follows the Phase 2 UI-SPEC 6px radius, no nested cards, and mobile no-overflow rules.
  </behavior>
  <action>Replace the Phase 1 event detail deferral with the real Event Workspace shell per D-01. Use `assertEventRole` to load the event for organisers, moderators, and speakers, and render role-appropriate tabs. Access panel must call access actions and show the manual onboarding notice. Keep surveys/results/exports out of Phase 2.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/event-workspace.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Accessible event workspace is usable by assigned staff roles, organiser access management works, and no Phase 3 surfaces are active destinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser to server action | Signed-in user requests member changes |
| server action to Supabase | Role mutation must be checked server-side and backed by RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Elevation of Privilege | `access-actions.ts` | mitigate | Require `assertEventRole(..., ["organiser"])` before invite/remove. |
| T-02-01-02 | Spoofing | workspace route params | mitigate | Resolve signed-in Supabase user server-side and ignore client-supplied role. |
| T-02-01-03 | Tampering | member removal | mitigate | Mark membership removed only after organiser check; block original organiser removal. |
| T-02-01-04 | Repudiation | pending member records | accept | Phase 2 has no email delivery; record created_at/status and avoid delivery claims. |
</threat_model>

<verification>
Run `npm run test -- tests/events/event-access.test.ts`, `npm run test:e2e -- tests/e2e/event-workspace.spec.ts`, and `npm run lint`.
</verification>

<success_criteria>
AUTH-05, AUTH-06, and AUTH-07 are satisfied through server-side helpers and workspace UI. No user reaches access, moderation, or presenter capabilities through client-only role checks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-event-qna-and-moderation/02-01-SUMMARY.md`.
</output>
