---
phase: 02-live-event-qna-and-moderation
plan: 07
type: execute
wave: 5
depends_on: ["02-01", "02-04", "02-05"]
files_modified:
  - "src/lib/qna/presenter.ts"
  - "src/app/(app)/events/[eventId]/presenter/page.tsx"
  - "src/components/qna/PresenterView.tsx"
  - "tests/qna/presenter.test.ts"
  - "tests/e2e/presenter-view.spec.ts"
autonomous: true
requirements: [AUTH-06, QNA-03, LIVE-03]
user_setup: []
must_haves:
  truths:
    - "Assigned speaker can open Presenter View"
    - "Presenter View shows approved live/answered questions only"
    - "Presenter View has no moderation, edit, submit, access, or participant identity controls"
  artifacts:
    - path: "src/lib/qna/presenter.ts"
      provides: "Presenter access and approved-only data helper"
      exports: ["getPresenterQuestions"]
    - path: "src/app/(app)/events/[eventId]/presenter/page.tsx"
      provides: "Authenticated presenter route"
    - path: "src/components/qna/PresenterView.tsx"
      provides: "Display-focused presenter UI"
  key_links:
    - from: "src/lib/qna/presenter.ts"
      to: "src/lib/events/access.ts"
      via: "getPresenterEventAccess"
      pattern: "getPresenterEventAccess"
    - from: "src/lib/qna/presenter.ts"
      to: "src/lib/supabase/rls.ts"
      via: "PUBLIC_QUESTION_STATUSES"
      pattern: "PUBLIC_QUESTION_STATUSES"
---

<objective>
Deliver approved-only Presenter View for assigned speakers and staff.

Purpose: Speakers need a focused screen, but speaker membership must not become a path to pending or archived question data.
Output: Presenter route/helper/component and tests.
</objective>

## Phase Goal

**As a** QSB event organiser or moderator, **I want to** collect questions through a join link, review them before they appear publicly, and show approved questions to participants and speakers, **so that** live event Q&A stays controlled and useful.

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-event-qna-and-moderation/02-CONTEXT.md
@.planning/phases/02-live-event-qna-and-moderation/02-UI-SPEC.md
@.planning/phases/02-live-event-qna-and-moderation/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-SOURCE-AUDIT.md
@.planning/phases/02-live-event-qna-and-moderation/02-01-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-04-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-05-SUMMARY.md
@src/lib/events/access.ts
@src/lib/qna/public.ts
@src/lib/supabase/rls.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove presenter access and visibility behavior</name>
  <files>tests/qna/presenter.test.ts, tests/e2e/presenter-view.spec.ts</files>
  <behavior>
    - Organiser, moderator, and speaker with active membership can access Presenter View.
    - Unassigned signed-in user sees access denied.
    - Presenter data helper selects only live/answered statuses.
    - Presenter markup contains vote counts and status labels, but no moderation controls or submit form.
  </behavior>
  <action>Create failing tests for AUTH-06, QNA-03, and LIVE-03. Include a regression case where a speaker is an event member and a pending row exists; the pending text must not be returned by the helper or rendered by E2E.</action>
  <verify>
    <automated>npm run test -- tests/qna/presenter.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/presenter-view.spec.ts</automated>
  </verify>
  <done>Tests lock presenter access and approved-only visibility before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement presenter helper and route</name>
  <files>src/lib/qna/presenter.ts, src/app/(app)/events/[eventId]/presenter/page.tsx</files>
  <behavior>
    - `getPresenterQuestions` checks presenter access using `getPresenterEventAccess`.
    - It selects public question fields only for live/answered rows.
    - Route renders access denied copy for unassigned users.
  </behavior>
  <action>Implement the presenter data path separately from moderation helpers per D-02 and D-08. Even organisers and moderators using Presenter View must receive only the presenter-safe question projection. Do not use the broad `questions_select_for_event_members` policy as the presenter data contract.</action>
  <verify>
    <automated>npm run test -- tests/qna/presenter.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Presenter route is authenticated, role-gated, and uses approved-only question data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build display-focused Presenter View UI</name>
  <files>src/components/qna/PresenterView.tsx</files>
  <behavior>
    - View shows event name, connection status slot, sort control, approved question list, vote count, Live/Answered status, and empty state.
    - Desktop question text uses display sizing from UI-SPEC; mobile falls back without overflow.
    - No admin shell controls, moderation controls, edit controls, submit controls, access controls, or participant identity management appear.
  </behavior>
  <action>Build a no-admin presenter component with large readable rows and safe sort options. Keep it in the authenticated app route but visually separate from the management shell. Do not implement question highlight because that is outside Phase 2 context.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/presenter-view.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Presenter View is readable, approved-only, and free of moderation or participant controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signed-in browser to presenter route | User claims event presenter access |
| presenter helper to database | Broad event membership policy could expose private rows if used carelessly |
| presenter component to display | Speaker-facing screen must not show moderation controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Information Disclosure | `getPresenterQuestions` | mitigate | Use `PUBLIC_QUESTION_STATUSES` and safe columns only. |
| T-02-07-02 | Elevation of Privilege | presenter route | mitigate | Require `getPresenterEventAccess` for organiser/moderator/speaker roles. |
| T-02-07-03 | Spoofing | route params | mitigate | Resolve signed-in Supabase user server-side; ignore client role claims. |
| T-02-07-04 | Information Disclosure | UI controls | mitigate | E2E asserts no moderation/access/submit controls render. |
</threat_model>

<verification>
Run presenter unit tests, presenter E2E tests, and lint.
</verification>

<success_criteria>
AUTH-06 and LIVE-03 are complete, and QNA-03 remains enforced for speakers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-event-qna-and-moderation/02-07-SUMMARY.md`.
</output>
