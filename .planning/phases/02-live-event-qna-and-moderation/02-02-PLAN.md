---
phase: 02-live-event-qna-and-moderation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "src/lib/events/settings.ts"
  - "src/app/(app)/events/[eventId]/settings-actions.ts"
  - "src/components/events/EventSettingsPanel.tsx"
  - "tests/events/event-settings.test.ts"
  - "tests/e2e/event-settings.spec.ts"
autonomous: true
requirements: [EVNT-04, EVNT-05, QNA-12]
user_setup: []
must_haves:
  truths:
    - "Organiser can edit event details and question rules from Event Workspace"
    - "Organiser can close and archive an event without deleting records"
    - "Turning moderation off requires the warning confirmation"
  artifacts:
    - path: "src/lib/events/settings.ts"
      provides: "Organiser-only event settings update and lifecycle helpers"
      exports: ["updateEventSettings", "closeEvent", "archiveEvent"]
    - path: "src/components/events/EventSettingsPanel.tsx"
      provides: "Settings form with moderation warning and lifecycle confirmations"
  key_links:
    - from: "src/app/(app)/events/[eventId]/settings-actions.ts"
      to: "src/lib/events/access.ts"
      via: "assertEventRole organiser check"
      pattern: "assertEventRole"
    - from: "src/components/events/EventSettingsPanel.tsx"
      to: "src/app/(app)/events/[eventId]/settings-actions.ts"
      via: "server actions"
      pattern: "(updateEventSettingsAction|closeEventAction|archiveEventAction)"
---

<objective>
Deliver organiser event settings, close, archive, and moderation-warning controls.

Purpose: Question submission depends on event rule fields being real and editable, while event lifecycle must preserve records.
Output: Organiser-only settings helpers/actions/UI plus tests.
</objective>

## Phase Goal

**As a** QSB event organiser or moderator, **I want to** collect questions through a join link, review them before they appear publicly, and show approved questions to participants and speakers, **so that** live event Q&A stays controlled and useful.

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-event-qna-and-moderation/02-CONTEXT.md
@.planning/phases/02-live-event-qna-and-moderation/02-UI-SPEC.md
@.planning/phases/02-live-event-qna-and-moderation/02-SOURCE-AUDIT.md
@.planning/phases/02-live-event-qna-and-moderation/02-01-SUMMARY.md
@src/lib/events/access.ts
@src/lib/events/validation.ts
@src/components/events/EventForm.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove settings and lifecycle behavior</name>
  <files>tests/events/event-settings.test.ts, tests/e2e/event-settings.spec.ts</files>
  <behavior>
    - Event settings accepts name, starts_at, time_zone, identity_mode, moderation_enabled, question_character_limit, duplicate_block_enabled, and question_rate_limit_seconds.
    - Character limit must be 50-1000 and rate limit 5-300.
    - Close sets status to ended and archive sets status to archived without deleting questions or members.
    - Moderation-off requires the exact warning copy before save.
  </behavior>
  <action>Create failing tests for EVNT-04, EVNT-05, and QNA-12. Reuse Phase 1 form/action testing style. E2E should open the Settings tab from `/events/event-1`, validate warning dialog copy, close confirmation, archive confirmation, and 360px no-overflow.</action>
  <verify>
    <automated>npm run test -- tests/events/event-settings.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/event-settings.spec.ts</automated>
  </verify>
  <done>Tests encode organiser-only settings edits, lifecycle status changes, validation ranges, and required moderation warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement organiser-only settings helpers and actions</name>
  <files>src/lib/events/settings.ts, src/app/(app)/events/[eventId]/settings-actions.ts</files>
  <behavior>
    - `updateEventSettings` validates all fields and calls `assertEventRole(..., ["organiser"])`.
    - `closeEvent` sets `events.status` to `ended`.
    - `archiveEvent` sets `events.status` to `archived`.
    - None of the helpers delete records.
  </behavior>
  <action>Implement settings parsing and server actions against the existing `events` table. Reuse `assertEventRole` from Plan 02-01 per D-03. Keep moderation enabled as the default; only allow moderation off when a submitted confirmation field proves the UI warning was acknowledged per D-02 and QNA-12.</action>
  <verify>
    <automated>npm run test -- tests/events/event-settings.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Settings and lifecycle helpers pass focused tests and enforce organiser role server-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Event Settings panel</name>
  <files>src/components/events/EventSettingsPanel.tsx</files>
  <behavior>
    - Settings tab shows event fields, identity mode, moderation toggle, character limit, duplicate block, rate limit, close, and archive actions.
    - Close/archive destructive confirmations use Phase 2 UI-SPEC copy.
    - Controls are unavailable to moderators and speakers.
  </behavior>
  <action>Build the settings panel as an internal Tailwind component. Use visible labels, inline field errors, form-level summary, confirmation dialogs, and 44px mobile controls. Do not add survey, export, deployment, or prolonged reconnect settings.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/event-settings.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Organisers can update event settings, close, and archive from the workspace; moderator/speaker views do not render settings controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser to settings action | Untrusted form fields mutate event rules and lifecycle |
| settings action to database | Event record changes affect public participation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | `updateEventSettings` | mitigate | Validate ranges and enum values server-side before update. |
| T-02-02-02 | Elevation of Privilege | settings actions | mitigate | Require organiser role through `assertEventRole`. |
| T-02-02-03 | Information Disclosure | settings panel | mitigate | Render controls only after server role gate; no client-only hiding. |
| T-02-02-04 | Repudiation | close/archive | accept | Status changes preserve records; exportable audit comes in Phase 3. |
</threat_model>

<verification>
Run focused settings tests, E2E settings tests, and lint.
</verification>

<success_criteria>
EVNT-04, EVNT-05, and QNA-12 are complete with organiser-only server enforcement and UI confirmations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-event-qna-and-moderation/02-02-SUMMARY.md`.
</output>
