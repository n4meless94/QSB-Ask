---
phase: 02-live-event-qna-and-moderation
plan: 06
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - "supabase/migrations/202605220203_qna_voting.sql"
  - "src/lib/qna/voting.ts"
  - "src/app/join/[joinCode]/qna/vote-actions.ts"
  - "src/components/qna/AudienceQuestionList.tsx"
  - "tests/qna/voting.test.ts"
  - "tests/e2e/audience-qna.spec.ts"
autonomous: true
requirements: [QNA-03, QNA-10, QNA-11]
user_setup: []
must_haves:
  truths:
    - "Participant sees approved live and answered questions only"
    - "Participant can sort approved questions by Popular or Recent"
    - "Participant can upvote an approved live question once per session"
  artifacts:
    - path: "src/lib/qna/voting.ts"
      provides: "Session-validated one-vote helper"
      exports: ["upvoteQuestion"]
    - path: "src/components/qna/AudienceQuestionList.tsx"
      provides: "Approved-only audience list, sort, vote controls"
    - path: "supabase/migrations/202605220203_qna_voting.sql"
      provides: "Atomic vote_count maintenance"
  key_links:
    - from: "src/lib/qna/voting.ts"
      to: "question_votes_one_per_session"
      via: "insert with unique constraint handling"
      pattern: "question_votes"
    - from: "src/components/qna/AudienceQuestionList.tsx"
      to: "src/lib/qna/public.ts"
      via: "approved-only data contract"
      pattern: "live|answered"
---

<objective>
Deliver audience approved-question list, sorting, and one-vote behavior.

Purpose: Audience engagement must remain safe: only approved questions are visible and voting cannot inflate counts.
Output: Voting helper/action, atomic vote_count maintenance, audience list UI, and tests.
</objective>

## Phase Goal

**As a** QSB event organiser or moderator, **I want to** collect questions through a join link, review them before they appear publicly, and show approved questions to participants and speakers, **so that** live event Q&A stays controlled and useful.

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-event-qna-and-moderation/02-CONTEXT.md
@.planning/phases/02-live-event-qna-and-moderation/02-UI-SPEC.md
@.planning/phases/02-live-event-qna-and-moderation/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-SOURCE-AUDIT.md
@.planning/phases/02-live-event-qna-and-moderation/02-04-SUMMARY.md
@src/lib/qna/public.ts
@src/lib/participants/session.ts
@supabase/migrations/202605220101_foundation_schema.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove voting and audience sorting behavior</name>
  <files>tests/qna/voting.test.ts, tests/e2e/audience-qna.spec.ts</files>
  <behavior>
    - Upvote accepts only valid participant session for the same event.
    - Upvote accepts live questions only; answered/archived/pending are rejected.
    - Second upvote by same session is idempotent or returns already-voted without incrementing.
    - `vote_count` matches the number of unique vote rows.
    - Popular sort orders vote_count desc then submitted_at desc; Recent sort orders submitted_at desc.
  </behavior>
  <action>Create failing tests for QNA-10 and QNA-11 plus the QNA-03 visibility invariant. E2E should assert pending and archived text never appears, answered questions show status and no active vote action, and mobile 360px has no horizontal overflow.</action>
  <verify>
    <automated>npm run test -- tests/qna/voting.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/audience-qna.spec.ts</automated>
  </verify>
  <done>Tests prove vote uniqueness, atomic count behavior, and audience approved-only sorting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic upvote helper and action</name>
  <files>supabase/migrations/202605220203_qna_voting.sql, src/lib/qna/voting.ts, src/app/join/[joinCode]/qna/vote-actions.ts</files>
  <behavior>
    - `upvoteQuestion` validates participant token and event ownership.
    - It inserts one `question_votes` row per session/question.
    - It updates `questions.vote_count` atomically only when a unique insert succeeds.
    - It does not permit votes on answered, pending, or archived questions.
  </behavior>
  <action>Add database trigger/function or RPC to maintain `vote_count` without read-modify-write drift. Handle unique-constraint collisions as already-voted state. Keep the helper server-only and never trust a posted participant_session_id per D-04 and D-07.</action>
  <verify>
    <automated>npm run test -- tests/qna/voting.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Voting is session-bound, live-only, one-per-session, and count-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build audience approved-question list</name>
  <files>src/components/qna/AudienceQuestionList.tsx</files>
  <behavior>
    - List renders only live/answered question rows from the safe public data shape.
    - Sort control supports Popular and Recent.
    - Vote button shows count, pressed state, disabled answered state, and accessible name.
    - Empty state copy matches UI-SPEC.
  </behavior>
  <action>Build the audience list component and connect it to the participant Q&A page from Plan 02-04. Do not show pending counts, moderation status tabs, edit history, participant emails, member roles, admin controls, survey tabs, or exports.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/audience-qna.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Participants can view, sort, and vote on approved live questions once, with answered rows visible but not votable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser to vote action | Anonymous participant requests vote |
| vote action to database | Unique vote and count update must be atomic |
| audience list to browser | Public-safe rows only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | `upvoteQuestion` | mitigate | Validate participant token and live question status server-side. |
| T-02-06-02 | Tampering | `vote_count` | mitigate | Use DB-side atomic count maintenance tied to successful unique insert. |
| T-02-06-03 | Information Disclosure | audience list | mitigate | Render only public helper result shape and test absence of private statuses. |
| T-02-06-04 | Denial of Service | repeated vote clicks | mitigate | Disable button while pending and rely on unique constraint for final authority. |
</threat_model>

<verification>
Run voting unit tests, audience Q&A E2E tests, lint, and database reset if migration changes apply.
</verification>

<success_criteria>
QNA-10 and QNA-11 are complete, and QNA-03 remains enforced on audience surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-event-qna-and-moderation/02-06-SUMMARY.md`.
</output>
