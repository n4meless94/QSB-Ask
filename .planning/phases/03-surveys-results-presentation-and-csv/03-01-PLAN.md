---
phase: "03-surveys-results-presentation-and-csv"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/surveys/validation.ts"
  - "src/lib/surveys/management.ts"
  - "src/app/(app)/events/[eventId]/survey-actions.ts"
  - "src/components/surveys/SurveyList.tsx"
  - "src/components/surveys/SurveyEditor.tsx"
  - "src/components/events/EventWorkspace.tsx"
  - "src/app/(app)/events/[eventId]/page.tsx"
  - "tests/surveys/management.test.ts"
  - "tests/e2e/surveys-management.spec.ts"
autonomous: true
requirements:
  - SURV-01
  - SURV-02
  - SURV-03
  - SURV-04
  - SURV-05
  - SURV-06
  - SURV-07
must_haves:
  truths:
    - "Organiser can create an event-scoped draft survey with a title."
    - "Organiser can add multiple choice, multiple select, rating, and open text questions."
    - "Organiser can publish only a valid survey and close a published survey."
    - "Participant result visibility is hidden by default and organiser-controlled."
  artifacts:
    - path: "src/lib/surveys/management.ts"
      provides: "Organiser-only survey lifecycle and question persistence helpers"
    - path: "src/components/surveys/SurveyEditor.tsx"
      provides: "Accessible organiser survey authoring UI"
    - path: "src/components/events/EventWorkspace.tsx"
      provides: "Event Workspace tabs for Q&A, Surveys, Results, Exports, Access, Settings, and Presenter"
  key_links:
    - from: "src/app/(app)/events/[eventId]/survey-actions.ts"
      to: "src/lib/surveys/management.ts"
      via: "server actions call organiser-only helpers"
      pattern: "createSurvey|saveSurveyDraft|publishSurvey|closeSurvey|saveSurveyVisibility"
    - from: "src/app/(app)/events/[eventId]/page.tsx"
      to: "src/components/events/EventWorkspace.tsx"
      via: "survey panel props"
      pattern: "surveysPanel"
---

<objective>
Create the organiser survey authoring slice for SURV-01 through SURV-07.

Purpose: Organisers need to author and control survey lifecycle inside the existing Event Workspace before participants can respond.
Output: Server-guarded survey management helpers, actions, workspace tabs, survey list/editor UI, and tests proving lifecycle validation and organiser-only access.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-CONTEXT.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-UI-SPEC.md
@.planning/phases/03-surveys-results-presentation-and-csv/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-03-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-06-SUMMARY.md
@src/app/(app)/events/[eventId]/page.tsx
@src/components/events/EventWorkspace.tsx
@src/lib/events/access.ts
@src/types/app.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove organiser survey lifecycle behavior</name>
  <files>tests/surveys/management.test.ts, tests/e2e/surveys-management.spec.ts</files>
  <behavior>
    - Test 1: organiser access is required before listing, creating, saving, publishing, closing, or changing visibility for surveys.
    - Test 2: publishing fails unless the survey has a nonblank title, at least one question, choice/select questions have at least two nonblank options, rating questions use 5 or 10, and open text questions have a prompt.
    - Test 3: result visibility defaults to hidden and can be changed only by an organiser.
    - Test 4: the Event Workspace exposes Surveys, Results, and Exports navigation without showing authoring controls to non-organiser fixture roles.
  </behavior>
  <action>Create focused Vitest and Playwright coverage first. Use mocks consistent with tests/qna/submission.test.ts and tests/qna/presenter.test.ts. The E2E spec should use the existing qsb_ask_e2e_auth fixture path and verify the UI-SPEC copy for "Create survey", "Publish survey", "Save draft", "Add question", "Only organisers can create and manage surveys for this event.", and "Survey is not ready to publish".</action>
  <verify>
    <automated>npm test -- tests/surveys/management.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/surveys-management.spec.ts</automated>
  </verify>
  <done>Tests express SURV-01 through SURV-07 and fail before implementation for missing survey management behavior or missing workspace UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement organiser-only survey management helpers and actions</name>
  <files>src/lib/surveys/validation.ts, src/lib/surveys/management.ts, src/app/(app)/events/[eventId]/survey-actions.ts</files>
  <behavior>
    - Test 1: helper calls assertEventRole with EVENT_MANAGEMENT_ROLES before any survey mutation.
    - Test 2: create survey inserts draft status with results_visible_to_participants false.
    - Test 3: save draft persists title, question type, prompt, option labels, rating scale, and positions for the approved question types only.
    - Test 4: publish and close transitions are server-confirmed; invalid publish returns field/action errors instead of changing status.
  </behavior>
  <action>Add a new server-only survey domain under src/lib/surveys. Implement validation as pure functions in validation.ts and database-facing organiser helpers in management.ts. Use assertEventRole with EVENT_MANAGEMENT_ROLES, createSupabaseServerClient, existing survey tables, and typed DTOs; do not rely on browser role filtering. Server actions in survey-actions.ts should call the helpers, return compact ok/message/errors payloads, and revalidate /events/{eventId}. Preserve the existing Supabase survey schema instead of recreating tables.</action>
  <verify>
    <automated>npm test -- tests/surveys/management.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Organiser lifecycle helpers and actions satisfy SURV-01 through SURV-07 with server-side access and publish validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Event Workspace survey authoring UI</name>
  <files>src/components/surveys/SurveyList.tsx, src/components/surveys/SurveyEditor.tsx, src/components/events/EventWorkspace.tsx, src/app/(app)/events/[eventId]/page.tsx</files>
  <behavior>
    - Test 1: organiser sees Surveys, Results, Exports, Access, Settings, and Presenter tabs in the UI-SPEC order.
    - Test 2: Surveys tab shows survey list, empty state, active editor, lifecycle status badge, result visibility control, and validation summary.
    - Test 3: controls meet mobile-friendly 360px layout expectations and use existing Button, Field, Badge, and Alert primitives.
    - Test 4: moderators and speakers do not see survey authoring controls.
  </behavior>
  <action>Extend EventWorkspace to accept surveysPanel, resultsPanel, and exportsPanel props and add tab labels in the required order. Wire EventDetailPage to load organiser survey summaries only for organiser role and pass an access-denied panel for other roles. Build SurveyList and SurveyEditor as dense operational Tailwind surfaces with no nested cards, no marketing layout, no shadcn, and no client-side-only authorization assumptions.</action>
  <verify>
    <automated>npm test -- tests/surveys/management.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/surveys-management.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Organisers can create, edit, publish, close, and configure visibility for surveys from the Event Workspace; non-organisers cannot access authoring controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organiser browser -> survey server actions | Untrusted form data crosses into organiser-only survey mutations. |
| server actions -> Supabase survey tables | Role-checked application code writes event-scoped survey records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Elevation of Privilege | src/lib/surveys/management.ts | mitigate | Call assertEventRole(userId,eventId,EVENT_MANAGEMENT_ROLES) before every list or mutation helper. |
| T-03-01-02 | Tampering | src/lib/surveys/validation.ts | mitigate | Validate title, question type, prompt, option counts, rating scale, and lifecycle transition server-side before database writes. |
| T-03-01-03 | Information Disclosure | src/app/(app)/events/[eventId]/page.tsx | mitigate | Load authoring data only for organiser role and render non-organiser access-denied copy instead of survey controls. |
</threat_model>

<verification>
Run the focused management tests and E2E spec, then lint:
- npm test -- tests/surveys/management.test.ts
- npm run test:e2e -- tests/e2e/surveys-management.spec.ts
- npm run lint
</verification>

<success_criteria>
SURV-01 through SURV-07 are implemented: organiser-only survey creation, approved question types, publish/close lifecycle, server validation, hidden-by-default result visibility, and Event Workspace authoring UI.
</success_criteria>

<output>
Create `.planning/phases/03-surveys-results-presentation-and-csv/03-01-SUMMARY.md` when done.
</output>
