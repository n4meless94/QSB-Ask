---
phase: "03-surveys-results-presentation-and-csv"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - "supabase/migrations/202605300302_survey_submission_rpc.sql"
  - "src/lib/supabase/database.types.ts"
  - "src/lib/surveys/participant.ts"
  - "src/app/join/[joinCode]/surveys/page.tsx"
  - "src/app/join/[joinCode]/surveys/submit-actions.ts"
  - "src/components/surveys/SurveySubmitForm.tsx"
  - "tests/surveys/participant.test.ts"
  - "tests/db/survey-submission-rpc.test.ts"
  - "tests/e2e/survey-submission.spec.ts"
autonomous: true
requirements:
  - SURV-08
  - SURV-09
must_haves:
  truths:
    - "Participant can open a published survey from the join flow."
    - "Participant can submit exactly one response per survey session."
    - "Participant does not see results unless organiser visibility is enabled."
    - "Draft and closed surveys never render enabled participant response controls."
  artifacts:
    - path: "supabase/migrations/202605300302_survey_submission_rpc.sql"
      provides: "Atomic survey response and answer insertion RPC"
    - path: "src/lib/surveys/participant.ts"
      provides: "Participant-safe survey loading and submission helpers"
    - path: "src/components/surveys/SurveySubmitForm.tsx"
      provides: "Mobile-first participant survey form"
  key_links:
    - from: "src/app/join/[joinCode]/surveys/submit-actions.ts"
      to: "src/lib/participants/session.ts"
      via: "HTTP-only participant cookie token validation"
      pattern: "validateParticipantSession"
    - from: "src/lib/surveys/participant.ts"
      to: "submit_survey_response"
      via: "trusted server-side RPC call after session validation"
      pattern: "rpc\\(\"submit_survey_response\""
---

<objective>
Create the participant survey submission slice for SURV-08 and SURV-09.

Purpose: Participants need a public, session-gated survey route that records one complete response and respects result visibility.
Output: Atomic submission migration/RPC, participant survey helper/action/route, mobile survey form, and tests proving one-response and hidden-results behavior.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-CONTEXT.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-UI-SPEC.md
@.planning/phases/03-surveys-results-presentation-and-csv/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-03-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-06-SUMMARY.md
@src/lib/participants/session.ts
@src/app/join/[joinCode]/qna/page.tsx
@tests/qna/submission.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove participant survey submission behavior</name>
  <files>tests/surveys/participant.test.ts, tests/db/survey-submission-rpc.test.ts, tests/e2e/survey-submission.spec.ts</files>
  <behavior>
    - Test 1: participant helper validates the event-scoped participant token before loading or submitting a survey.
    - Test 2: published surveys render answer controls; draft and closed surveys render unavailable states.
    - Test 3: one response per participant session is enforced and duplicate submission returns "You have already submitted this survey."
    - Test 4: participant results are hidden unless results_visible_to_participants is true.
    - Test 5: migration text defines submit_survey_response and keeps survey_responses_one_per_session intact.
  </behavior>
  <action>Create participant unit, migration-text, and E2E tests before implementation. Mirror the compact action-result pattern from Q&A submission tests and use the existing qsb_ask_e2e_auth fixture mode for public route interaction. The E2E spec must cover 360px mobile width, no organiser controls, hidden-results notice, completed state, and duplicate-submit state.</action>
  <verify>
    <automated>npm test -- tests/surveys/participant.test.ts tests/db/survey-submission-rpc.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/survey-submission.spec.ts</automated>
  </verify>
  <done>Tests express SURV-08 and SURV-09 plus the required DB/RLS safety checks and fail before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic participant survey persistence</name>
  <files>supabase/migrations/202605300302_survey_submission_rpc.sql, src/lib/supabase/database.types.ts, src/lib/surveys/participant.ts, src/app/join/[joinCode]/surveys/submit-actions.ts</files>
  <behavior>
    - Test 1: submission inserts one survey_responses row and one survey_answers row per answered survey question in a single database-side operation.
    - Test 2: server helper validates question answer shape by type before calling the RPC.
    - Test 3: duplicate unique-constraint results become the already-submitted message.
    - Test 4: action reads the participant cookie through getParticipantCookieName(eventId) and never trusts participant_session_id from form data.
  </behavior>
  <action>Add a migration that creates a service-role-intended submit_survey_response RPC for response plus answer rows and grants execution only as appropriate for server-side use. Update database.types.ts with generated-style RPC typing manually if local Supabase generation is unavailable. Implement participant.ts to load only published surveys for a valid participant session, validate answer shapes for multiple_choice, multiple_select, rating, and open_text, call the RPC after validateParticipantSession, and expose visibility-aware completion state.</action>
  <verify>
    <automated>npm test -- tests/surveys/participant.test.ts tests/db/survey-submission-rpc.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Participant survey submissions are session-gated, atomic, duplicate-aware, and do not expose hidden results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build participant survey route and form</name>
  <files>src/app/join/[joinCode]/surveys/page.tsx, src/components/surveys/SurveySubmitForm.tsx</files>
  <behavior>
    - Test 1: /join/{joinCode}/surveys renders event name, survey title, question groups, and Submit survey for published surveys.
    - Test 2: successful submit clears transient form state and shows "Survey submitted. Thank you. Your response has been recorded for this event."
    - Test 3: failed submit preserves entered answers and displays inline error copy.
    - Test 4: public markup does not include organiser controls, staff metadata, raw response ids, participant emails, token hashes, or raw tokens.
  </behavior>
  <action>Create the public join survey route outside the authenticated app shell. Build SurveySubmitForm with semantic fieldsets for choice/select/rating/open-text inputs, mobile touch targets, visible labels, loading/error/completion states, and hidden-results notice. Reuse Button, Field, Alert, and the Phase 2 participant page style; do not import EventWorkspace or organiser management helpers into the public route.</action>
  <verify>
    <automated>npm test -- tests/surveys/participant.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/survey-submission.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Participants can submit one response to published surveys and see only visibility-approved result state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant browser -> survey submit action | Untrusted public form data crosses into server-side participant session validation. |
| submit action -> Supabase RPC | Trusted server code writes survey response and answers after validating cookie-token possession. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | src/app/join/[joinCode]/surveys/submit-actions.ts | mitigate | Read raw token only from HTTP-only participant cookie and validate with validateParticipantSession. |
| T-03-02-02 | Tampering | supabase/migrations/202605300302_survey_submission_rpc.sql | mitigate | Keep survey_responses_one_per_session and use RPC transaction semantics for response plus answer rows. |
| T-03-02-03 | Information Disclosure | src/lib/surveys/participant.ts | mitigate | Return participant-visible results only when results_visible_to_participants is true and never include raw identifiers. |
</threat_model>

<verification>
Run:
- npm test -- tests/surveys/participant.test.ts tests/db/survey-submission-rpc.test.ts
- npm run test:e2e -- tests/e2e/survey-submission.spec.ts
- npm run lint
</verification>

<success_criteria>
SURV-08 and SURV-09 are implemented: participants can submit one response per survey session, duplicate submissions are handled clearly, and participant results remain hidden unless organiser visibility allows them.
</success_criteria>

<output>
Create `.planning/phases/03-surveys-results-presentation-and-csv/03-02-SUMMARY.md` when done.
</output>
