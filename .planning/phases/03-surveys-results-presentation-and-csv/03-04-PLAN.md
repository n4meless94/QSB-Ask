---
phase: "03-surveys-results-presentation-and-csv"
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - "supabase/migrations/202605300304_survey_realtime.sql"
  - "src/lib/surveys/realtime.ts"
  - "src/lib/surveys/results.ts"
  - "src/app/(app)/events/[eventId]/presentation/surveys/[surveyId]/page.tsx"
  - "src/app/join/[joinCode]/surveys/page.tsx"
  - "src/components/surveys/SurveyPresentationView.tsx"
  - "src/components/surveys/SurveySubmitForm.tsx"
  - "tests/db/survey-realtime.test.ts"
  - "tests/e2e/survey-presentation.spec.ts"
autonomous: true
requirements:
  - LIVE-05
  - SURV-09
  - SURV-13
must_haves:
  truths:
    - "Presentation View shows survey charts without admin controls."
    - "Presentation View updates survey result data within 2 seconds in normal conditions."
    - "Participant-visible results show only when organiser visibility is enabled."
    - "Presentation and participant result surfaces receive aggregate DTOs only."
  artifacts:
    - path: "supabase/migrations/202605300304_survey_realtime.sql"
      provides: "Supabase Realtime publication entries for survey tables"
    - path: "src/lib/surveys/realtime.ts"
      provides: "Survey realtime subscription helpers"
    - path: "src/components/surveys/SurveyPresentationView.tsx"
      provides: "Display-focused survey chart surface"
  key_links:
    - from: "src/components/surveys/SurveyPresentationView.tsx"
      to: "src/lib/surveys/realtime.ts"
      via: "client refresh callback and ConnectionStatus"
      pattern: "subscribeToSurveyResults"
    - from: "src/lib/surveys/results.ts"
      to: "src/app/join/[joinCode]/surveys/page.tsx"
      via: "participant visibility-gated aggregate helper"
      pattern: "getParticipantVisibleSurveyResults"
---

<objective>
Create survey presentation and participant-visible result refresh for LIVE-05, SURV-09, and SURV-13.

Purpose: Staff need a display-focused survey chart view and participants may see results only under organiser-controlled visibility, both refreshed safely under normal live conditions.
Output: Realtime migration, subscription helper, presentation route/UI, participant-visible aggregate result wiring, and tests for no-admin-controls, no data leakage, and 2-second fixture refresh.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-CONTEXT.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-UI-SPEC.md
@.planning/phases/03-surveys-results-presentation-and-csv/RESEARCH.md
@.planning/phases/02-live-event-qna-and-moderation/02-07-SUMMARY.md
@.planning/phases/02-live-event-qna-and-moderation/02-08-SUMMARY.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-02-SUMMARY.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-03-SUMMARY.md
@src/lib/qna/realtime.ts
@src/components/qna/ConnectionStatus.tsx
@tests/e2e/qna-realtime.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove survey presentation and realtime-safe visibility behavior</name>
  <files>tests/db/survey-realtime.test.ts, tests/e2e/survey-presentation.spec.ts</files>
  <behavior>
    - Test 1: migration text adds surveys, survey_responses, and survey_answers to the Supabase Realtime publication without removing Q&A publication entries.
    - Test 2: presentation route shows event name, survey title, connection state, response count, charts/tables, and zero-response state.
    - Test 3: presentation route has no admin shell, survey editor, export controls, raw participant identifiers, token hashes, or raw tokens.
    - Test 4: fixture-mode refresh event updates result counts within 2 seconds and does not trust raw hidden-result payloads.
    - Test 5: participant route shows aggregate results only when result visibility is enabled.
  </behavior>
  <action>Create migration-text and E2E tests first. Follow qna-realtime.spec.ts by using custom browser events only in fixture mode to prove UI refresh within 2 seconds without requiring live Supabase Realtime. Assert absence of admin controls and private identifiers as explicitly as presence of chart labels.</action>
  <verify>
    <automated>npm test -- tests/db/survey-realtime.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/survey-presentation.spec.ts</automated>
  </verify>
  <done>Tests express LIVE-05, SURV-09, and SURV-13 and fail before implementation for missing realtime publication or presentation surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement survey realtime publication and aggregate visibility helpers</name>
  <files>supabase/migrations/202605300304_survey_realtime.sql, src/lib/surveys/realtime.ts, src/lib/surveys/results.ts</files>
  <behavior>
    - Test 1: migration adds survey tables to publication idempotently and keeps Q&A publication entries intact.
    - Test 2: subscribeToSurveyResults listens to survey_responses and survey_answers for the selected survey and calls onRefresh.
    - Test 3: participant-visible results helper returns null or hidden state unless results_visible_to_participants is true.
    - Test 4: public/presentation helpers return aggregate DTOs and not raw survey_answers rows.
  </behavior>
  <action>Add survey realtime publication SQL, mirroring the Phase 2 Q&A realtime migration style. Implement src/lib/surveys/realtime.ts using Supabase browser channel().on("postgres_changes") and the existing live/reconnecting/refresh-needed state names. Extend results.ts with getPresentationSurveyResults and getParticipantVisibleSurveyResults, keeping role/session and visibility checks separate from organiser results.</action>
  <verify>
    <automated>npm test -- tests/db/survey-realtime.test.ts tests/surveys/results.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Survey realtime and visibility helpers preserve safe aggregate boundaries and support normal-condition refresh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build survey presentation view and participant visible-results state</name>
  <files>src/app/(app)/events/[eventId]/presentation/surveys/[surveyId]/page.tsx, src/app/join/[joinCode]/surveys/page.tsx, src/components/surveys/SurveyPresentationView.tsx, src/components/surveys/SurveySubmitForm.tsx</files>
  <behavior>
    - Test 1: authenticated presenter route uses presenter/staff access rules and renders display-focused aggregate survey results.
    - Test 2: SurveyPresentationView includes ConnectionStatus, charts, tables, response count, zero-response state, and fixture refresh support.
    - Test 3: participant survey completion state renders visible aggregate results only when enabled and hidden-results notice otherwise.
    - Test 4: presentation and participant result surfaces remain usable at mobile widths without horizontal scrolling.
  </behavior>
  <action>Create the presentation route under the authenticated event tree and use getPresenterEventAccess-style role checks before loading aggregate result DTOs. Build SurveyPresentationView without EventWorkspace, admin shell, editor controls, or export controls. Update the participant survey page/form to render participant-visible aggregate results after submission only when helper output allows it. Use router.refresh for realtime callbacks, not raw payload rendering.</action>
  <verify>
    <automated>npm test -- tests/db/survey-realtime.test.ts tests/surveys/results.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/survey-presentation.spec.ts tests/e2e/survey-submission.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>LIVE-05 is met for survey Presentation View, and participant result visibility remains organiser-controlled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| realtime payload -> browser UI | Browser receives table-change notifications but must refresh safe server data. |
| presenter route -> aggregate result helper | Authenticated staff access controls gate display data. |
| participant route -> aggregate result helper | Participant session and visibility flag gate public result data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | src/components/surveys/SurveyPresentationView.tsx | mitigate | Render aggregate DTOs only and assert no raw identifiers, editor controls, or export controls in E2E. |
| T-03-04-02 | Spoofing | src/app/(app)/events/[eventId]/presentation/surveys/[surveyId]/page.tsx | mitigate | Require presenter/staff access before loading presentation results. |
| T-03-04-03 | Tampering | src/lib/surveys/realtime.ts | mitigate | Treat realtime events as refresh triggers only; rebuild UI from server helpers. |
</threat_model>

<verification>
Run:
- npm test -- tests/db/survey-realtime.test.ts tests/surveys/results.test.ts
- npm run test:e2e -- tests/e2e/survey-presentation.spec.ts tests/e2e/survey-submission.spec.ts
- npm run lint
</verification>

<success_criteria>
LIVE-05, SURV-09, and SURV-13 are implemented for display and participant surfaces: charts have accessible data alternatives, presentation updates within 2 seconds in fixture-normal conditions, and no public/presentation view receives hidden raw data.
</success_criteria>

<output>
Create `.planning/phases/03-surveys-results-presentation-and-csv/03-04-SUMMARY.md` when done.
</output>
