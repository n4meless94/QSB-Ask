---
phase: "03-surveys-results-presentation-and-csv"
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - "package.json"
  - "package-lock.json"
  - "src/lib/surveys/results.ts"
  - "src/components/surveys/SurveyBarChart.tsx"
  - "src/components/surveys/OpenTextResponseList.tsx"
  - "src/components/surveys/SurveyResultsPanel.tsx"
  - "src/app/(app)/events/[eventId]/page.tsx"
  - "tests/surveys/results.test.ts"
  - "tests/e2e/survey-results.spec.ts"
autonomous: true
requirements:
  - SURV-10
  - SURV-11
  - SURV-12
  - SURV-13
must_haves:
  truths:
    - "Organiser can view response counts for each survey and question."
    - "Organiser can view chart results for choice and rating questions."
    - "Organiser can view open text responses in a readable data view."
    - "Every chart has readable labels, values, percentages, and an adjacent accessible data table."
  artifacts:
    - path: "package.json"
      provides: "Approved Recharts 3.x dependency"
    - path: "src/lib/surveys/results.ts"
      provides: "Server-derived aggregate DTOs"
    - path: "src/components/surveys/SurveyBarChart.tsx"
      provides: "Recharts chart wrapper plus accessible table"
    - path: "src/components/surveys/SurveyResultsPanel.tsx"
      provides: "Organiser results UI"
  key_links:
    - from: "src/lib/surveys/results.ts"
      to: "survey_responses/survey_answers"
      via: "server aggregation before UI props"
      pattern: "SurveyResult"
    - from: "src/components/surveys/SurveyBarChart.tsx"
      to: "recharts"
      via: "ResponsiveContainer and BarChart wrapper"
      pattern: "ResponsiveContainer|BarChart"
---

<objective>
Create the organiser results and accessible chart slice for SURV-10 through SURV-13.

Purpose: Organisers need response counts, chart summaries, and open text data views before presentation and export surfaces can reuse safe result DTOs.
Output: Approved Recharts dependency, aggregate result helper, chart/table components, organiser results panel, and tests for counts, charts, open text, and accessibility alternatives.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-CONTEXT.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-UI-SPEC.md
@.planning/phases/03-surveys-results-presentation-and-csv/RESEARCH.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-01-SUMMARY.md
@.planning/phases/03-surveys-results-presentation-and-csv/03-02-SUMMARY.md
@src/lib/events/access.ts
@src/components/qna/ConnectionStatus.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove organiser result aggregation and chart accessibility</name>
  <files>tests/surveys/results.test.ts, tests/e2e/survey-results.spec.ts</files>
  <behavior>
    - Test 1: organiser result helper returns survey response counts and per-question counts.
    - Test 2: choice, multiple-select, and rating questions return label, count, and percentage DTOs.
    - Test 3: open text questions return staff-only text rows and do not expose token hashes or raw tokens.
    - Test 4: chart UI renders readable labels, values, percentages, and adjacent table alternatives.
    - Test 5: zero-response surveys show "No responses yet" while preserving chart/table structure.
  </behavior>
  <action>Create result unit and E2E tests first. Use mocked Supabase query chains in the existing Vitest style and deterministic fixture data for E2E. The E2E spec must assert the Results tab, response count, chart labels, table values, open text rows, zero-response copy, and no horizontal overflow at 360px.</action>
  <verify>
    <automated>npm test -- tests/surveys/results.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/survey-results.spec.ts</automated>
  </verify>
  <done>Tests express SURV-10 through SURV-13 and fail before implementation for missing aggregation or chart/table UI.</done>
</task>

<task type="auto">
  <name>Task 2: Install approved Recharts dependency</name>
  <files>package.json, package-lock.json</files>
  <action>Install `recharts@3.8.1` with npm because RESEARCH.md includes a Package Legitimacy Audit marking Recharts OK and the approved stack names Recharts 3.x. Do not add any chart dependency besides Recharts. Keep package-lock.json updated by npm.</action>
  <verify>
    <automated>npm ls recharts</automated>
    <automated>npm test -- tests/surveys/results.test.ts</automated>
  </verify>
  <done>package.json and package-lock.json include Recharts 3.x, and npm can resolve the installed package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement server aggregates and organiser results UI</name>
  <files>src/lib/surveys/results.ts, src/components/surveys/SurveyBarChart.tsx, src/components/surveys/OpenTextResponseList.tsx, src/components/surveys/SurveyResultsPanel.tsx, src/app/(app)/events/[eventId]/page.tsx</files>
  <behavior>
    - Test 1: getOrganiserSurveyResults requires organiser access and returns aggregate DTOs rather than raw rows to components.
    - Test 2: SurveyBarChart uses Recharts for visual bars and always renders an adjacent table with the same labels, counts, and percentages.
    - Test 3: OpenTextResponseList wraps long text and never renders participant emails outside organiser-only results.
    - Test 4: Event Workspace Results tab includes survey selector/list, status, response count, visibility state, presentation link, and zero-response state.
  </behavior>
  <action>Implement results.ts as the server aggregation boundary for organiser results. Build SurveyBarChart as a small wrapper over Recharts ResponsiveContainer/BarChart with visible labels and a table alternative required by UI-SPEC. Build OpenTextResponseList and SurveyResultsPanel using existing internal UI primitives and Tailwind styling. Wire the Event Detail page to pass organiser-only results data into EventWorkspace; non-organisers must not receive raw response data.</action>
  <verify>
    <automated>npm test -- tests/surveys/results.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/survey-results.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Organiser Results tab satisfies SURV-10 through SURV-13 with Recharts visual charts and accessible data alternatives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase answer rows -> result DTOs | Staff-only raw answer rows become aggregate chart/data DTOs before rendering. |
| package registry -> app dependency | npm installs Recharts package into the application supply chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | src/lib/surveys/results.ts | mitigate | Keep raw answer rows inside server helper scope and pass aggregate DTOs plus organiser-only open text rows only to organiser results UI. |
| T-03-03-02 | Tampering | src/components/surveys/SurveyBarChart.tsx | mitigate | Render chart values and table values from the same DTO array so visual labels cannot diverge from accessible data. |
| T-03-SC | Tampering | npm install recharts | mitigate | Use audited `recharts@3.8.1` from RESEARCH.md Package Legitimacy Audit; do not install unaudited chart packages. |
</threat_model>

<verification>
Run:
- npm ls recharts
- npm test -- tests/surveys/results.test.ts
- npm run test:e2e -- tests/e2e/survey-results.spec.ts
- npm run lint
</verification>

<success_criteria>
SURV-10 through SURV-13 are implemented: organiser response counts, chart results, open text data view, visible chart labels/values/percentages, and accessible data table alternatives.
</success_criteria>

<output>
Create `.planning/phases/03-surveys-results-presentation-and-csv/03-03-SUMMARY.md` when done.
</output>
