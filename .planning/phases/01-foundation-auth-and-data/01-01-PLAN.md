---
phase: 01-foundation-auth-and-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - eslint.config.mjs
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/api/health/route.ts
  - src/components/ui/Button.tsx
  - src/components/ui/Field.tsx
  - src/components/ui/Alert.tsx
  - src/components/ui/Badge.tsx
  - src/components/shell/AppShell.tsx
  - src/lib/env.ts
  - src/types/app.ts
  - tests/e2e/foundation.spec.ts
  - playwright.config.ts
  - vitest.config.ts
  - .env.example
  - README.md
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - "A developer can install dependencies and run the app locally with documented environment variables."
    - "The app exposes a machine-readable health route for future Coolify checks."
    - "The initial UI shell matches the Phase 1 operational UI contract without marketing layout."
  artifacts:
    - path: "package.json"
      provides: "Next.js 16, TypeScript, Tailwind, lint, test, and local run scripts"
      contains: "next"
    - path: ".env.example"
      provides: "Documented Supabase and app runtime variables"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "src/app/api/health/route.ts"
      provides: "Machine-readable health endpoint"
      exports: ["GET"]
    - path: "tests/e2e/foundation.spec.ts"
      provides: "Walking-skeleton smoke coverage"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "local setup instructions"
      pattern: "NEXT_PUBLIC_SUPABASE_URL"
    - from: "src/lib/env.ts"
      to: "process.env"
      via: "validated runtime configuration"
      pattern: "process\\.env"
    - from: "tests/e2e/foundation.spec.ts"
      to: "/api/health"
      via: "Playwright request check"
      pattern: "api/health"
---

<objective>
Create the executable app foundation for Phase 1.

Purpose: This gives every later Phase 1 plan a running Next.js/Tailwind project, shared UI primitives, environment validation, local documentation, and a health target.
Output: Scaffolded app, local run docs, reusable primitive components, and foundation smoke tests.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-foundation-auth-and-data/01-CONTEXT.md
@.planning/phases/01-foundation-auth-and-data/01-UI-SPEC.md
@SPEC.md
@SRS.md

<interfaces>
No application source exists before this plan. Create contracts that later plans can import:
- `src/lib/env.ts`: export server-safe env parsing for `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SITE_URL`, `APP_SESSION_IDLE_TIMEOUT_SECONDS`, and `APP_JOIN_URL_BASE`.
- `src/types/app.ts`: export shared literal unions for `EventStatus`, `IdentityMode`, and `EventRole`.
- `src/components/ui/*`: export small Tailwind primitives only; do not initialize shadcn or add registry blocks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the app and failing foundation smoke test</name>
  <files>package.json, package-lock.json, next.config.ts, tsconfig.json, eslint.config.mjs, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, playwright.config.ts, vitest.config.ts, tests/e2e/foundation.spec.ts</files>
  <behavior>
    - Test 1: `GET /api/health` should return status 200 with JSON containing `ok: true`, `service: "qsb-ask"`, and no secrets.
    - Test 2: Visiting `/` should render a working app page with a single `h1` and no horizontal overflow at 360px width.
    - Test 3: The page should not contain marketing hero copy, decorative gradients, or nested cards banned by the Phase 1 UI-SPEC.
  </behavior>
  <action>Initialize the repository as a Next.js 16 App Router project with TypeScript, Tailwind CSS, ESLint, Vitest, and Playwright. Use current framework conventions from Context7: App Router under `src/app`, Tailwind global import/configuration, and server-only env access only on the server. Write the Playwright smoke test before implementing the health route so RED is observable. Follow the 01-CONTEXT App Foundation decision and 01-UI-SPEC: quiet operational UI, no landing page, no hero, no decorative gradients, no shadcn registry.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/foundation.spec.ts</automated>
  </verify>
  <done>Project installs and the foundation smoke test initially fails for the missing health route before Task 2 completes.</done>
</task>

<task type="auto">
  <name>Task 2: Add health route, environment contract, and local setup docs</name>
  <files>src/app/api/health/route.ts, src/lib/env.ts, .env.example, README.md, package.json</files>
  <action>Create `GET /api/health` returning machine-readable JSON with service, ok, environment, and timestamp fields while never exposing secrets. Add strict env parsing in `src/lib/env.ts` and use it only from server-safe code unless a variable is explicitly `NEXT_PUBLIC_*`. Document local setup in `README.md`: install, copy `.env.example`, configure Supabase URL/anon key/service role key/site URL/join URL base/session idle timeout, run dev, run tests. Satisfy DEPL-01 and the 01-CONTEXT health-route decision; do not add Coolify deployment steps beyond noting Phase 4 owns deployment.</action>
  <verify>
    <automated>npm run lint && npm run test && npm run test:e2e -- tests/e2e/foundation.spec.ts</automated>
  </verify>
  <done>A developer can run the app locally from README instructions, missing env variables are documented, and `/api/health` returns valid non-secret JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Add operational UI primitives and app shell baseline</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Field.tsx, src/components/ui/Alert.tsx, src/components/ui/Badge.tsx, src/components/shell/AppShell.tsx, src/app/layout.tsx, src/app/page.tsx, src/types/app.ts</files>
  <action>Create reusable primitives needed by Phase 1 screens: Button, Field, Alert, Badge, and AppShell. Match 01-UI-SPEC exactly: Inter/Arial/sans font stack, 6px radius, teal accent `#0F766E`, visible focus, semantic landmarks, one h1 per page, 40px desktop and 44px mobile control targets, no nested cards. Redirect or link the root page toward the future auth/dashboard flow without implementing auth here.</action>
  <verify>
    <automated>npm run lint && npm run test:e2e -- tests/e2e/foundation.spec.ts</automated>
  </verify>
  <done>Shared UI primitives exist, compile cleanly, and the app shell visually follows the Phase 1 UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to Next.js route handler | Public requests reach `/api/health` and initial pages. |
| environment to server runtime | Secrets enter through local `.env` or future Coolify variables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | I | `src/app/api/health/route.ts` | mitigate | Return only non-secret operational fields; never serialize env values. |
| T-01-01-02 | T | `src/lib/env.ts` | mitigate | Validate required variables at runtime and fail with explicit missing-key names, not secret values. |
| T-01-01-03 | D | local dev scripts | accept | Local-only dev server availability is low risk; README documents restart and env repair steps. |
</threat_model>

<verification>
Run `npm run lint`, `npm run test`, and `npm run test:e2e -- tests/e2e/foundation.spec.ts`.
</verification>

<success_criteria>
DEPL-01 is satisfied for Phase 1: the app can be installed, configured with documented env vars, run locally, and checked through a non-secret health route.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-and-data/01-01-SUMMARY.md`.
</output>
