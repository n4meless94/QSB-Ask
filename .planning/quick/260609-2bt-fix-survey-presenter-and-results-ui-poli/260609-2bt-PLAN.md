---
status: planned
quick_id: 260609-2bt
slug: fix-survey-presenter-and-results-ui-poli
created: 2026-06-08
---

# Quick Task: Fix Survey Presenter And Results UI Polish

## Goal

Fix survey presenter/result UI issues called out from screenshot review:
- presenter tile text must not clip,
- presenter bar mode must not horizontally scroll,
- dashboard chart labels must stay readable,
- survey selector active state must be obvious,
- empty and low-response states must make sparse results feel intentional.

## Tasks

1. Update `src/components/surveys/SurveyPresentationView.tsx` so tile labels wrap within cards, bar rows fit without horizontal overflow, and low-sample state appears in presenter mode.
2. Update `src/components/surveys/SurveyBarChart.tsx` so dashboard charts do not rely on cramped y-axis labels and show clear empty/low-response context.
3. Update `src/components/surveys/SurveyResultsPanel.tsx` so selected survey cards are more visible and low-response context is surfaced near survey summaries.

## Verification

- Run focused lint/type checks for touched survey components.
- Run relevant survey tests or focused E2E where feasible.
- Launch local app and visually inspect presenter/results surfaces in browser if the repo supports the fixture route.
